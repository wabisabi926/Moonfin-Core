name: moonfin_design
description: Shared design system - theme tokens, colors, typography, and reusable widgets.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  # Glass renderer. Pinned exact because a bump changes the look and wants
  # checking on device first.
  liquid_glass_widgets: 1.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
