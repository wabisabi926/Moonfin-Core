name: custom_tv_text_field
description: Moonfin fork of custom_tv_text_field — TV-optimized on-screen keyboard. Hidden native input, password masking added.
version: 1.1.0
publish_to: none

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: '>=3.32.0'

dependencies:
  flutter:
    sdk: flutter
  moonfin_design:
    path: ../design

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
