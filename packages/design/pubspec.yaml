name: moonfin_design
description: Shared design system - theme tokens, colors, typography, and reusable widgets.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  # Glass renderer. Pinned exact because pre-1.0 minor releases can break.
  liquid_glass_widgets: 0.22.1

dev_dependencies:
  flutter_test:
    sdk: flutter
